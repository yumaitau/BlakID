# Application catalogue

Templates live in `packages/integrations`. Milestone 1 ships the catalogue as data plus OIDC create. Live Entra, Google, SCIM and SAML wizards are Milestone 2.

LDAP entries must be labelled legacy in the UI.
