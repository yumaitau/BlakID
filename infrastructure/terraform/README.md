# Terraform

Describes the Australian default: AWS `ap-southeast-2`, ECS cluster for the control plane. This goal does not apply the stack to production.

Customer identity stacks are dedicated authentik+Postgres services, not a shared ECS service talking to one database.
